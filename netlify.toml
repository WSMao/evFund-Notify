[build]
  command = "npm run build"
  publish = "dist"

# SPA 路由支援 - 將所有路由重定向到 index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[plugins]]
  package = "@netlify/plugin-lighthouse"
